# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Rust build artifacts + Tauri's generated (non-authored) schema files
src-tauri/target
src-tauri/gen

# Local-only Cargo overrides (e.g. a machine-specific build-cache target-dir) — must not ship,
# they break CI and other clones.
src-tauri/.cargo/config.toml

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
